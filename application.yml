server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dhirajlingayat/config-repo 
          clone-on-start: true
          search-paths: "{application}"
  application:
    name: config-server
